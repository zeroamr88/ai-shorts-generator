{
  "name": "Python FFmpeg Env",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },

  "postCreateCommand": "sudo apt update && sudo apt install ffmpeg -y && pip install --upgrade pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },

  "remoteUser": "vscode"
}
